import 'package:amal/service/constant/appfinals.dart';
import 'package:flutter/material.dart';

class Dimensions {
  Dimensions._();

  static double width = MediaQuery.of(AppFinals.context).size.width;
  static double height = MediaQuery.of(AppFinals.context).size.height;
}
